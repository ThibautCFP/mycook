<header class="sticky-top">
	<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		<div class="container-fluid">
			<a href="{{ path('app.home') }}" class="navbar-brand">MyCook38</a>
			<ul class="navbar-nav me-auto">
				<li
					class="navbar-item">{# <a href="" class="nav-link">Produits</a> #}
				</li>
				<li class="navbar-item">
					<a href="{{ path('app.contact.index') }}" class="nav-link {{ app.request.get('_route') == 'app.contact.index' ? 'active' }}">Nous contacter</a>
				</li>
				<li class="navbar-item">
					<a href="{{ path('app.devis.create') }}" class="nav-link {{ app.request.get('_route') == 'app.devis.create' ? 'active' }}">Devis</a>
				</li>
			</ul>
		</div>
		<div class="collapse navbar-collapse">
			{% if app.user %}
				{% if is_granted('ROLE_ADMIN') %}
					<!-- Example single danger button -->
					<div class="btn-group">
						<button type="button" class="btn btn-warning dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
							Gestion
						</button>
						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{ path('app.admin.products.index') }}">
									<i class="bi bi-shop"></i>
									Produits</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app.admin.contacts.index') }}">
									<i class="bi bi-card-list"></i>
									Contacts</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app.admin.devis.index') }}">
									<i class="bi bi-envelope-paper"></i>
									Devis</a>
							</li>
							{# <li><hr class="dropdown-divider"></li>
																																																															<li>
																																																																<a class="dropdown-item" href="#">Separated link</a>
																																																															</li> #}
						</ul>
					</div>

				{% endif %}
				<div class="me-2">
					<a href="{{ path('app.logout') }}" class="btn btn-danger">Déconnexion</a>
				</div>
			{% else %}
				<div class="me-2 d-flex">
					<a href="{{ path('app.login') }}" class="btn btn-light">Connexion</a>
					<a href="{{ path('app.register') }}" class="btn btn-info">S'enregistrer</a>
				</div>
			{% endif %}
		</div>
	</nav>
</header>
