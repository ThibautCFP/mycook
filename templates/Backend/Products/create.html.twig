{% extends "base.html.twig" %}

{% block title %}
	Création produit
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-4">
		<div class="card p-3 w-50 mx-auto">
			<h1 class="text-center">Création d'un produit</h1>
			{% include "Backend/Products/_form.html.twig" with {button_label: 'Créer'} %}

		</div>
		<a href="{{ path('app.admin.products.index') }}" class="btn btn-primary">
			<i class="bi bi-arrow-return-left"></i>
			Retour à la liste</a>
	</section>

{% endblock %}
